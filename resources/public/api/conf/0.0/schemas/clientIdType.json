{
  "type": "string",
  "description": "The type of identifier that clientId is",
  "oneOf": [
    {
      "enum": [
        "ni"
      ],
      "description": "National Insurance Number"
    },
    {
      "enum": [
        "vrn"
      ],
      "description": "VAT registration number"
    }
  ]
}
