{
  "type": "object",
  "description": "Creates a new invitation between the agency and client",
  "properties": {
    "service": {
      "$ref": "service.json"
    },
    "clientIdType": {
      "$ref": "clientIdType.json"
    },
    "clientId": {
      "type": "string",
      "description" : "Client identifier (of type clientIdType)"
    },
    "knownFact": {
      "type": "string",
      "description" : "Known fact matching client identifier, required for some services"
    }
  },
  "required": [
    "service",
    "clientIdType",
    "clientId"
  ]
}